# Port-security_to_Protect_the_network_within
🖧 I created a simple topology that shows how port-security won't allow the bad user. Port-security limits the mac addressess allowed in a switchport(interface).
